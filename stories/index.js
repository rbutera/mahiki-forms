import React from "react";
import { storiesOf } from "@storybook/react";
import { BookingForm } from "../src/bookingform";

storiesOf("Booking Form", module).add("Event Enquiry", () => <BookingForm />);
